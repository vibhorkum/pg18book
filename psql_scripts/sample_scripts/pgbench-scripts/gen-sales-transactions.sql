/*
-- =================================================================================
Command being called by pgbench scripts to randomly generate a sales transaction.

-- =================================================================================
*/

-- generate_random_sales_transaction() is defined in pgbench-stored-procedures.sql
CALL generate_random_sales_transaction();